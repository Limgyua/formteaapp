import 'package:flutter/material.dart';

class EtcScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('기타')),
      body: Center(child: Text('기타 페이지입니다')),
    );
  }
}
