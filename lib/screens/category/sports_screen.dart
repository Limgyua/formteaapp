import 'package:flutter/material.dart';

class SportsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('스포츠/레저')),
      body: Center(child: Text('스포츠/레저 페이지입니다')),
    );
  }
}
