import 'package:flutter/material.dart';

class GameScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('게임/취미')),
      body: Center(child: Text('게임/취미 페이지입니다')),
    );
  }
}
