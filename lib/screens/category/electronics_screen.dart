import 'package:flutter/material.dart';

class ElectronicsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('전자제품')),
      body: Center(child: Text('전자제품 페이지입니다')),
    );
  }
}
