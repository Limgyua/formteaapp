import 'package:flutter/material.dart';

class LivingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('생활용품')),
      body: Center(child: Text('생활용품 페이지입니다')),
    );
  }
}
