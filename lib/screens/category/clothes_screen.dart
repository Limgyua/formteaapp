import 'package:flutter/material.dart';

class ClothingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('의류')),
      body: Center(child: Text('의류 페이지입니다')),
    );
  }
}
