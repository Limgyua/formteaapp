import 'package:flutter/material.dart';

class BikeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('자전거')),
      body: Center(child: Text('자전거 페이지입니다')),
    );
  }
}
