import 'package:flutter/material.dart';

class ShoppingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('쇼핑')),
      body: Center(child: Text('쇼핑 화면')),
    );
  }
}
