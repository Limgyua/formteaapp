name: formteaapp
description: A new Flutter project.

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # 예: cupertino_icons: ^1.0.2
  sqflite: ^2.4.2
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/kakao.png
    - assets/google.png
    - assets/naver.png
